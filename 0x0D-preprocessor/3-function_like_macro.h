#ifndef ABS_MACRO_H
#define ABS_MACRO_H

/**
* ABS - A macro that computes the absolute value of a number x.
* @x: The number for which the absolute value is calculated.
*
* Return: The absolute value of x.
*/
#define ABS(x) ((x) < 0 ? -(x) : (x))

#endif /* ABS_MACRO_H */
